# Art source workspace

High-resolution editable source art belongs here and should be tracked with Git LFS when practical.
Runtime exports belong under `public/assets/` and must be registered in `public/assets/manifest.json`.
Never export a low-resolution runtime image and then upscale it to satisfy the visual bar.
